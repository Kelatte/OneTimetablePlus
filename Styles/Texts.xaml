<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:local="clr-namespace:OneTimetablePlus.Styles">

    <Style TargetType="{x:Type TextBlock}" x:Key="BaseText" BasedOn="{StaticResource BaseTextBlockStyle}">
        <Setter Property="VerticalAlignment" Value="Center"/>
        <Setter Property="HorizontalAlignment" Value="Center"/>
        <Setter Property="TextAlignment" Value="Center"/>
    </Style>
    <Style TargetType="{x:Type TextBlock}" x:Key="MiddleText" BasedOn="{StaticResource BaseText}">

        <Setter Property="FontSize" Value="20"/>
    </Style>

    <Style TargetType="{x:Type TextBlock}" x:Key="SmallText" BasedOn="{StaticResource BaseText}">
        <Setter Property="FontSize" Value="15"/>
    </Style>

    <Style TargetType="{x:Type TextBlock}" x:Key="SmallerText" BasedOn="{StaticResource BaseText}">
        <Setter Property="FontSize" Value="13"/>
    </Style>

    <Style TargetType="{x:Type TextBlock}" x:Key="LargeText" BasedOn="{StaticResource BaseText}">
        <Setter Property="FontSize" Value="32"/>
    </Style>

    <Style TargetType="{x:Type TextBlock}" x:Key="LargerText" BasedOn="{StaticResource BaseText}">
        <Setter Property="FontSize" Value="60"/>
    </Style>
</ResourceDictionary>